[project]
name = "lstm-word-prediction"
version = "0.1.0"
description = "LSTM Word Prediction"
requires-python = "==3.11.*"
dependencies = [
    "streamlit",
    "tensorflow-cpu",
    "numpy",
]

[tool.uv]
package = false
